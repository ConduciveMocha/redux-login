import { put, takeEvery, call } from 'redux-saga/effects';
import {
    CHECK_USERNAME_EXISTS, 
    CHECK_EMAIL_EXISTS, 
    USERNAME_EXISTS,
    EMAIL_EXISTS} from '../constants/registration-types';

export function* usernameExistsSaga(action) {
    try {
        
    }

    catch (err) {

    }
}

export function* emailExistsSaga(action){

}


